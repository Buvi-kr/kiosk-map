# 포천아트밸리 키오스크 경로안내 시스템 (운영 가이드)

## 📌 개요
포천아트밸리를 방문하는 관람객을 위한 무인 키오스크(1080×1920 세로형 터치 디스플레이) 경로 안내 웹 애플리케이션입니다.
인터넷 연결 없이 **단일 HTML 파일과 배경 이미지 파일**만으로 구동되며, 직관적인 길찾기 인터랙션 및 고화질 애니메이션을 제공합니다.

---

## 📁 필수 구성 파일

키오스크 구동을 위해 아래 파일들이 **반드시 같은 폴더(디렉토리)** 안에 위치해야 합니다:
- `index.html` (메인 키오스크 웹 애플리케이션 파일)
- `아트밸리_전경도_안내_v2.jpg` (3337 × 2101 초고화질 안내도 배경 이미지)
- `천문과학관_로고.png` (상단 헤더 좌측 공식 로고)
- `안내프로그램.bat` (어떤 PC에서나 원클릭으로 전체화면 키오스크 구동하는 자동 런처)
- `test.json` (공식 경로 백업 데이터)

---

## 🚀 실행 방법

### 방법 A: 원클릭 자동 실행 (가장 추천 ⭐)
* `안내프로그램.bat`을 더블클릭합니다.
* 키오스크 PC의 계정명/설치 경로/브라우저 종류(Edge 32/64bit, Chrome 등)를 **자동 감지하여 완전한 전체화면 키오스크 모드로 즉시 실행**됩니다.

### 방법 B: 수동 브라우저 실행
1. 웹 브라우저(Chrome, Edge 등)로 `index.html`을 실행합니다.
2. 브라우저에서 **전체화면(F11)** 모드로 전환하여 운영합니다. (완전 오프라인 구동)

---

## ⚙️ 설치 장소(단말기)별 시작 위치 설정

키오스크가 설치된 위치에 따라 현위치를 자동 설정할 수 있습니다.

### 방법 A: 파일 이름 변경 (추천)
- **천문과학관 기기:** `아트밸리_경로안내_천문과학관.html`
- **매표소 기기:** `아트밸리_경로안내_매표소.html`
- **관광정보센터 기기:** `아트밸리_경로안내_관광정보센터.html`

### 방법 B: 브라우저 URL 파라미터 사용
- 천문과학관: `.../아트밸리_경로안내.html?start=astro`
- 매표소: `.../아트밸리_경로안내.html?start=ticket`
- 관광정보센터: `.../아트밸리_경로안내.html?start=info`

---

## ✨ 주요 기능 및 핵심 구현 사항

1. **인쇄본 노란 점(Vector Dot) 100% 실측 앵커 동기화**
   * 안내도 이미지에 인쇄된 각 시설물의 노란 원형 앵커 점(`천문과학관`, `천주호`, `전망카페`, `산마루공연장`, `모노레일 승·하차장`, `조각공원`, `힐링숲`, `검표소` 등)의 실제 픽셀 좌표를 정밀 매핑하여 경로 선이 시설물 앵커로 정확히 이어집니다.

2. **PDF 300 DPI 초고화질 헤더 제거 배경 (3337 × 2101)**
   * PDF 벡터 원본에서 상단 종이용 인쇄 띠를 제거하고 300 DPI로 무손실 추출하여, 2~3배 줌인 시에도 글자와 아이콘이 칼같이 선명합니다.

3. **목적지별 고유 테마 컬러 & 서브 글로우 버튼 시스템**
   * 천주호(시안), 카페(앰버 오렌지), 모노레일(코발트 블루), 힐링숲(에메랄드 그린)의 독립 테마 컬러를 적용하여 멀리서도 직관적 인지 가능.

4. **텍스트 중복 없는 네온 펄스 마커 링 UI & 복수 경로 분기**
   * 목적지에 시선이 집중되는 초대형 네온 펄스 마커 링을 띄우며, 보행로와 계단길 등 복수 코스를 동시 주행 안내합니다.

---

## 🔐 관리자 모드 및 경로 수정 (제작 모드)

1. **진입 방법:** 화면 **[우측 하단 모서리]**를 **2초 안에 빠르게 5번 연속 터치/클릭**합니다.
2. **경로 수정:**
   * **`[🖊 경로 제작 모드]`** 클릭
   * 시작 위치(`매표소`, `천문과학관`, `관광정보센터`) 및 목적지 버튼 선택
   * 지도 위를 클릭하여 새로운 경로 점(Point)들을 순서대로 연결
   * `[+ 경로 추가]`: 파란색 대안 경로 추가
   * `[↩ 취소]`: 마지막 점 취소 / `[🗑 초기화]`: 전체 초기화
3. **내보내기 & 영구 저장:**
   * `[⇩ 내보내기]` 클릭 후 생성된 JSON 코드를 복사하여 `ALL_ROUTES`에 붙여넣고 저장합니다.

---

## 🛡️ 무인 키오스크 안정성 및 자율 안내 기능 (하드닝)

- **터치 줌/더블탭 줌 차단:** 관람객의 임의 확대 조작 차단
- **롱프레스(우클릭) 차단:** 컨텍스트 메뉴 및 텍스트 선택 방지
- **20초 유휴 자동 복귀:** 경로 안내 실행 후 20초간 추가 터치가 없으면 전체 지도(초기 상태)로 자동 복귀
- **1분 무인 자동 순환 안내 (어트랙트 롤링):** 1분간 아무런 조작이 없을 시 `천주호` ➔ `전망카페` ➔ `모노레일` ➔ `힐링숲` 버튼을 1분 간격으로 순차적 자율 안내 (화면 터치 시 즉시 사용자 제어로 전환)

---

## 📜 버전 변경 내역 (Version History)

* **v1.0.0 ~ v3.0.0**: 초기 실사 조감도 기반 단일/복수 경로 및 관광정보센터 시작점 도입
* **v4.2.0**: 
  * PDF 300 DPI 마스터 기반 초고화질 헤더 제거 배경(`3337 × 2101`) 및 인쇄 노란 점 실측 앵커 동기화
  * 코스 1 / 코스 2 복수 경로 동시 발광 주행 및 힐링 숲 산책로 초록 스마트 분기
  * 도착지 앵커 자동 스냅 & 초대형 고가시성 핀 렌더링
  * 플로팅 정량 줌 컨트롤러(➕/➖/⟲) & 마우스 휠 줌
* **v4.3.0 (최신)**:
  * **상단 헤더 좌상단 공식 로고(`천문과학관_로고.png`) 배치**
  * **헤더 타이틀 가시성 극대화** (아이스 블루 그라디언트 + 골드 포인트 + 백라이트 글로우) 및 중복 서브타이틀 정리
  * **목적지별 테마 컬러 & 서브 글로우 UI** (천주호/카페/모노레일/힐링숲 독립 네온 시스템)
  * **키오스크 표준 안내 문구 정돈** (어색한 문장 일괄 간결화)
